name: Build php
on:
  push:
    branches: [ master ]
    paths:
      - 'php/**'
      - '.github/workflows/php.yml'
jobs:
  build:
    name: Build container
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v2
      - uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: manual_kaniko_build
        run: >
          docker run
          -v ~/.docker:/kaniko/.docker
          -v $(pwd)/php:/context
          gcr.io/kaniko-project/executor@sha256:57f2f8bd18c05f9cdfec586cf6d3a85f962163fc20e984bfa39bd14234cf176c
          --context dir:///context/
          --destination ghcr.io/herams-who/docker/php:${{ github.sha }}
          --destination ghcr.io/herams-who/docker/php:latest
          --build-arg COMMIT_SHA=${{ github.sha }}
          --use-new-run
