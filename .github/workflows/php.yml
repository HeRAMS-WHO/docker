name: Build php
on:
  push:
    branches: [ master ]
    paths:
      - 'php/**'
      - '.github/workflows/php.yml'
jobs:
  build:
    name: Build container
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v2
      - uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: manual_kaniko_build
        run: >
          docker run
          -v ~/.docker:/kaniko/.docker
          -v $(pwd)/php:/context
          gcr.io/kaniko-project/executor@99a09a7153a35346604b91b7499547e608505430
          --context dir:///context/
          --destination ghcr.io/herams-who/docker/php:${{ github.sha }}
          --destination ghcr.io/herams-who/docker/php:latest
          --build-arg COMMIT_SHA=${{ github.sha }}
          --use-new-run
